interface:
  display_name: "Directory Architecture"
  short_description: "Design and verify domain-oriented module boundaries"
  default_prompt: "Use $directory-architecture to design or audit this project structure and its dependency boundaries."
policy:
  allow_implicit_invocation: true
